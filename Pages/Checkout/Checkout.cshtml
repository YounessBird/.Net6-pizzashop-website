@page
@model CheckoutModel
@{
ViewData["Title"] = "Checkout";
}

<h1>You Order!</h1>
@{
var ImagePath="~/Images/Pizzas/" + @Model.ImageTitle + ".png";
}
<div class="align-content-center">
    <div class="col-3 mx-auto mb-5">
        <img src=@ImagePath alt="" class="w-100" asp-append-version="true" />
        <p>Your order for the @Model.PizzaName piza is ready! </p>
        <p>Total price for your order is: £@Model.PizzaPrice </p>
        <br />
        <a class="btn btn-primary mt-1 mx-auto" asp-page="Thankyou">Confirm Order!</a>
    </div>
</div>